import {FetchCuisinesData} from '../../services/api/fetchCuisines/types';

// TODO: use a good library to transform responses to entities to save in store
export function normalizeFetchCuisines(response: FetchCuisinesData) {
  return {};
}
